require 'spec_helper'

describe AiGame do

  before do
    @game = AiGame.new
  end

  #it "plays a move when a player makes a move" do
  #  @game.move('X', 4)
  #  expect(@game.display_board).to include 'x'
  #  expect(@game.display_board).to include 'o'
  #end
end
